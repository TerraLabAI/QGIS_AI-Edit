"""The pages of the AI Edit settings dialog (AI Agent's left-rail layout).

``account_settings_dialog.AccountSettingsDialog`` is the frame: the rail, the
stack and the account load. The pages build from the mixins here:

- ``account_page``: identity card, usage, Advanced and the danger zone.
- ``account_deletion``: the erasure request and its refusal sentences.
- ``billing_page``: the plan card, the Manage card, the Pro offer.
- ``help_pages``: Tutorials, Keyboard shortcuts, More plugins, Contact us.
- ``widgets``: the page frame, rows, groups, the switch, the billing card.
"""
